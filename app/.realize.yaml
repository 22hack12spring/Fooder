settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: app
  path: .
  commands: {}
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
